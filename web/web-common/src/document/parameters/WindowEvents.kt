package org.solvo.web.document.parameters


object WindowEvents {
    val EVENT_LOCATION_CHANGE = "locationChange".lowercase()
    val EVENT_POP_STATE = "popState".lowercase()
    val EVENT_PUSH_STATE = "pushState".lowercase()
}